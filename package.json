{
  "name": "waldo-tabs",
  "version": "0.2.0",
  "private": true,
  "description": "Agentic tab conductor — resource tracking, hibernation, AI automation",
  "scripts": {
    "lint": "web-ext lint --self-hosted",
    "test": "node --test tests/*.test.js",
    "test:e2e": "node --test tests/e2e/*.test.js",
    "test:watch": "node --test --watch tests/*.test.js"
  },
  "devDependencies": {
    "geckodriver": "^4.0.0",
    "jsdom": "^26.0.0",
    "selenium-webdriver": "^4.0.0",
    "web-ext": "^8.0.0"
  }
}
